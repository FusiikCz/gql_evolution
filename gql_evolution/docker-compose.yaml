x-services-volume:
  &system-data-volume
  type: bind
  source: ./systemdata.rnd.json
  target: /app/systemdata.json


services:

  chatgpt-web:
    image: yidadaa/chatgpt-next-web
    environment:
      OPENAI_API_KEY: <Azure OpenAI API Key>
      BASE_URL: http://azure_proxy:8000
      CODE: ""
      HIDE_USER_API_KEY: 1
      HIDE_BALANCE_QUERY: 1
      ENABLE_MCP: "true"
    ports:
      - 3000:3000

  gql_app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
      POSTGRES_DB: data
      POSTGRES_HOST: postgres_gql:5432
      DEMODATA: "True"
      DISABLE_WHOAMI_EXTENSION: "False"
    depends_on:
      - postgres_gql
    ports:
      - 8000:8000
    volumes:
      - ./:/app

  azure_proxy:
    build:
      context: .                    # potrebuje pristup k /src
      dockerfile: azure_proxy/Dockerfile
    depends_on:
      - postgres_gql
    environment:
        AZURE_COGNITIVE_ACCOUNT_NAME: ${AZURE_COGNITIVE_ACCOUNT_NAME}
        AZURE_OPENAI_API_KEY: ${OPENAI_API_KEY}
        AZURE_OPENAI_API_VERSION: ${AZURE_OPENAI_API_VERSION}
        POSTGRES_HOST: postgres_gql:5432
    command: ["uvicorn", "azure_proxy.main:app", "--host", "0.0.0.0", "--port", "8000"]
    volumes:
      - ./azure_proxy:/app/azure_proxy
      - ./src:/app/src
    ports:
      - 8798:8000

  apollo_router:
    image: ghcr.io/apollographql/router:v1.45.0
    ports:
      - 4000:4000
    environment:
      APOLLO_ROUTER_CONFIG_PATH: /config/router.yaml
    volumes:
      - ./apollo_config:/config
    depends_on:
      - gql_app
    command: --config /config/router.yaml

  postgres_gql:
    image: pgvector/pgvector:pg17
    environment:
      # promenne, ktere lze cist v kontejneru
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=example
      - POSTGRES_DB=data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 60s
      timeout: 10s
      retries: 5
    restart: on-failure:3 # nedojde k restartu pokud je kontejner "unhealthy", maximalne 3 pokusy
    # volumes:
      # - dbdata:/var/lib/postgresql/data
      # - ./pgvector:/docker-entrypoint-initdb.d    
    ports:
      - 5434:5432

  pgadmin:
    image: dpage/pgadmin4
    restart: always
    environment:
      # promenne, ktere lze cist v kontejneru
      - PGADMIN_DEFAULT_EMAIL=anoyne@anywhere.com
      - PGADMIN_DEFAULT_PASSWORD=example
    healthcheck:
      test: ["CMD", "wget", "-O", "-", "http://localhost:80/misc/ping"]
      interval: 60s
      timeout: 10s
      retries: 5
    ports:
      - 31800:80
